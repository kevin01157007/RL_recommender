name: gcntest
channels:
  - pyg
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pandas
  - pytest
  - faiss-cpu
  - torchvision
  - pytorch-cuda=11.8
  - pyg  # 這個包會安裝 torch-scatter, torch-sparse, torch-cluster, torch-spline-conv, torch-geometric